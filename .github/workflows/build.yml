name: Build NAV appliance
on: [push, pull_request]

jobs:
  packer:
    name: "Build appliance using Packer"
    runs-on: macOS-10.15
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Validate Template
        run: |
          packer validate buster.json
        env:
          PACKER_LOG: 0

      - name: Build Artifact
        run: |
          packer build buster.json
        env:
          PACKER_LOG: 1
          PACKER_LOG_PATH: /tmp/packer.log

      - name: Grep for nav package
        run: |
          grep 5.4.0-1 /tmp/packer.log

      - name: Upload appliance artifact
        uses: actions/upload-artifact@v2
        with:
          name: navappliance
          path: |
            navappliance/**/*
